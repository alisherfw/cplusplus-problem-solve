#include <bits/stdc++.h>
using namespace std;
int diskriminant(int a, b, c, D)
{
    D=(b*b)-4*a*c;
}
int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    if (D<0) cout << "0";
    if (D==0) cout << "1";
    if (D>0) cout << "2";
}
